(function (window, undefined) {
    'use strict';

    var elem = document.querySelector('.main-gallery');
    var flkty = new Flickity( elem, {
        cellAlign: 'left',
        contain: true
    });

})(window, undefined);
